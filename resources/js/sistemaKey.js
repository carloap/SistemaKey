$(document).ready(function() {
	/*
	var $meuMenu = $('header.main_header section.navigator'); // guardar o elemento na memoria para melhorar performance
	var $corpo = $('#container div.main_content');
	var offset = $meuMenu.offset().top; // distancia no topo
	$(window).on('scroll', function() {
		var pos = $(window).scrollTop();
		// fixar aba superior dinamicamente
		if (offset <= pos) {
			// $meuMenu.css({ position:'fixed' });
			$meuMenu.addClass('fixar');
			$corpo.addClass('contSegmentado');
		} else {
			// $meuMenu.css({ position:'relative' });
			$meuMenu.removeClass('fixar');
			$corpo.removeClass('contSegmentado');
		}
		// rolar o background do cenário
		$('body').css( {
			'background-position' : 'center ' + parseInt(pos / 2) + 'px'
		});
	});
 	*/
	
	
	
});